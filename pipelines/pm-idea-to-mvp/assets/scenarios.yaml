# pm-idea-to-mvp decompose scenarios (v7.1.1)
# Used by decompose-pm-pipeline.py --scenario

version: "7.1.1"

scenarios:
  greenfield:
    description: "New product idea — full 12-step outer loop + MVP inner loop"
    builder: greenfield

  brownfield:
    description: "Existing pm-{slug} project — audit then targeted MVP/ship"
    builder: brownfield

  optimize:
    description: "Deploy/fix/hotfix on existing project — minimal tasks"
    builder: optimize

  refine:
    description: "UX/journey/prototype refinement — spec-focused subset"
    builder: refine
