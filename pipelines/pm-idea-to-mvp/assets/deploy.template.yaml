# Non-secret deploy config for pm-{slug}
# server: pick from HERMES_HOME/config/deploy-servers.yaml (key_path + password_env)
# See docs/DEPLOY_CONVENTIONS.md
version: "1.0"
server: dc1-priority
host: ""
port: 22
user: ""
project_path: "~/pm-{slug}"
processes: {}
health_urls: []
secret_refs:
  ssh_key: "~/.ssh/id_ed25519_deploy"
  ssh_password: "servers/dc1-priority"
